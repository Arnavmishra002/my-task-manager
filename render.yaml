services:
  # Backend Service
  - type: web
    name: task-manager-server
    env: docker
    rootDir: server
    dockerfilePath: ./server/Dockerfile
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        value: file:./dev.db

  # Frontend Service (Converted to Web Service for Stability)
  - type: web
    name: task-manager-client
    env: node
    rootDir: client
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: task-manager-server
          property: host
